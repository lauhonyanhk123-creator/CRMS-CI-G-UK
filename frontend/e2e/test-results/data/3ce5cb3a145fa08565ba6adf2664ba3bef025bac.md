# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: login.spec.ts >> Login Form - Functional Tests >> form is interactive
- Location: e2e/login.spec.ts:74:3

# Error details

```
Test timeout of 30000ms exceeded.
```

```
Error: locator.click: Test timeout of 30000ms exceeded.
Call log:
  - waiting for locator('input').first()

```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test'
  2  | 
  3  | const BASE_URL = process.env.BASE_URL || 'http://localhost:5173'
  4  | 
  5  | test.describe('Login Page - UI Component Tests', () => {
  6  |   test('login page loads', async ({ page }) => {
  7  |     await page.goto(`${BASE_URL}/login`)
  8  |     await expect(page).toHaveTitle(/CRMS|Login|Sign In/)
  9  |   })
  10 | 
  11 |   test('shows form elements', async ({ page }) => {
  12 |     await page.goto(`${BASE_URL}/login`)
  13 |     await page.waitForLoadState('networkidle')
  14 |     const usernameInput = page.locator('input').first()
  15 |     await expect(usernameInput).toBeVisible()
  16 |   })
  17 | 
  18 |   test('has password field', async ({ page }) => {
  19 |     await page.goto(`${BASE_URL}/login`)
  20 |     await page.waitForLoadState('networkidle')
  21 |     const passwordInput = page.locator('input[type="password"]')
  22 |     await expect(passwordInput.first()).toBeVisible()
  23 |   })
  24 | 
  25 |   test('has submit button', async ({ page }) => {
  26 |     await page.goto(`${BASE_URL}/login`)
  27 |     await page.waitForLoadState('networkidle')
  28 |     const submitButton = page.locator('button[type="submit"], button:has-text("Sign In"), button:has-text("Login")')
  29 |     await expect(submitButton.first()).toBeVisible()
  30 |   })
  31 | 
  32 |   test('no critical console errors', async ({ page }) => {
  33 |     const errors: string[] = []
  34 |     page.on('console', msg => {
  35 |       if (msg.type() === 'error') {
  36 |         const text = msg.text()
  37 |         if (!text.includes('[Vue warn]') && !text.includes('Failed to fetch')) {
  38 |           errors.push(text)
  39 |         }
  40 |       }
  41 |     })
  42 |     await page.goto(`${BASE_URL}/login`)
  43 |     await page.waitForLoadState('networkidle')
  44 |     expect(errors).toHaveLength(0)
  45 |   })
  46 | })
  47 | 
  48 | test.describe('Login Form - Functional Tests', () => {
  49 |   test('demo login buttons exist', async ({ page }) => {
  50 |     await page.goto(`${BASE_URL}/login`)
  51 |     await page.waitForLoadState('networkidle')
  52 |     const adminBtn = page.locator('button:has-text("Admin")')
  53 |     if (await adminBtn.isVisible()) {
  54 |       await expect(adminBtn).toBeVisible()
  55 |     }
  56 |   })
  57 | 
  58 |   test('username field accepts input', async ({ page }) => {
  59 |     await page.goto(`${BASE_URL}/login`)
  60 |     await page.waitForLoadState('networkidle')
  61 |     const usernameInput = page.locator('input').first()
  62 |     await usernameInput.fill('test@test.com')
  63 |     await expect(usernameInput).toHaveValue('test@test.com')
  64 |   })
  65 | 
  66 |   test('password field accepts input', async ({ page }) => {
  67 |     await page.goto(`${BASE_URL}/login`)
  68 |     await page.waitForLoadState('networkidle')
  69 |     const passwordInput = page.locator('input[type="password"]').first()
  70 |     await passwordInput.fill('password123')
  71 |     await expect(passwordInput).toHaveValue('password123')
  72 |   })
  73 | 
  74 |   test('form is interactive', async ({ page }) => {
  75 |     await page.goto(`${BASE_URL}/login`)
  76 |     await page.waitForLoadState('networkidle')
  77 |     const usernameInput = page.locator('input').first()
> 78 |     await usernameInput.click()
     |                         ^ Error: locator.click: Test timeout of 30000ms exceeded.
  79 |     await usernameInput.fill('admin@crms.com')
  80 |     const passwordInput = page.locator('input[type="password"]').first()
  81 |     await passwordInput.click()
  82 |     await passwordInput.fill('admin123')
  83 |     expect(true).toBe(true)
  84 |   })
  85 | })
  86 | 
```