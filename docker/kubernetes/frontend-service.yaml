# CRMS CI G UK - Frontend Service
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: crms
  labels:
    app: frontend
    component: web
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
